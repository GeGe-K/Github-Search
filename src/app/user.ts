export class User {
    constructor(public name:string, public avatar_url:string, public bio:string, public followes:number, public following: number, public repos:number, public email:string, public html_url:string, public repo_url:string, public created_at:Date ){}
}
